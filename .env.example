# 複製這個檔案為 .env，並填入實際的值
# Supabase 專案的 anon key 雖然是 public 可以放在前端，但仍建議用 .env 管理以區分環境
#
# Vite 規定前端可讀取的環境變數必須以 VITE_ 開頭

VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# ── 僅供後端 / 腳本使用（圖片上傳 scripts/sync-kv-images.mjs）──
# service_role key 權限極大、可繞過 RLS：
#   ‼ 不要加 VITE_ 前綴、不要放進前端、不要進版控（.env 已被 .gitignore）
# URL 腳本會自動沿用上面的 VITE_SUPABASE_URL，不必重設（要分開也可設 SUPABASE_URL）
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here
